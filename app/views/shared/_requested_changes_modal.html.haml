.btn.btn-info.btn-info--modal.btn-xs{ data: { target: '#requestedChangeModal', toggle: 'modal' }, type: 'button' }
  %i.glyphicon.glyphicon-question-sign
#requestedChangeModal.modal.fade{ aria: { labelledby: 'requestedChangeModalLabel' }, role: 'dialog', tabindex: '-1' }
  .modal-dialog{ role: 'document' }
    .modal-content
      .modal-header
        %button.close{ aria: { label: 'Close' }, data: { dismiss: 'modal' }, type: 'button' }
          %span{ aria: { hidden: 'true' } } ×
        %h4#categoryModalLabel.modal-title Requested changes
      .modal-body
        %span Table represents all requested changes. We have two types of requested change:
        %ul
          %li
            %b.text-primary create
          %li
            %b.text-info update
        %span Each request change has status. We have three different statuses:
        %ul
          %li
            %b.text-info created
            %span - when requested change is new, and ready to review.
          %li
            %b.text-success accepted
            %span - when requested change is accepted.
          %li
            %b.text-danger declined
            %span - when requested change is declined.
      .modal-footer
        %button.btn.btn-primary{ data: { dismiss: 'modal' }, type: 'button' } Got it!
