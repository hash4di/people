.btn.btn-info.btn-info--modal.btn-xs{ data: { target: '#categoryModal', toggle: 'modal' }, type: 'button' }
  %i.glyphicon.glyphicon-question-sign
#categoryModal.modal.fade{ aria: { labelledby: 'categoryModalLabel' }, role: 'dialog', tabindex: '-1' }
  .modal-dialog{ role: 'document' }
    .modal-content
      .modal-header
        %button.close{ aria: { label: 'Close' }, data: { dismiss: 'modal' }, type: 'button' }
          %span{ aria: { hidden: 'true' } } ×
        %h4#categoryModalLabel.modal-title Skill categories
      .modal-body
        %span Skill categories represents different sections at Netguru. Each section collets skills which we want to have as a company. We have one more special category.
        %b Shared
        %span which contains similar skills for couple categories.
      .modal-footer
        %button.btn.btn-primary{ data: { dismiss: 'modal' }, type: 'button' } Got it!
